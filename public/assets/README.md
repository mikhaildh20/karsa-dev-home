# Karsa Home Assets

Place the dynamic homepage logo here.

Default database value:

```txt
/assets/logo.png
```

Recommended file name:

```txt
logo.png
```

Change `logo_path` in PostgreSQL database `KarsaHome`, table `mst_detail_settings`, to point to another file path if needed.
